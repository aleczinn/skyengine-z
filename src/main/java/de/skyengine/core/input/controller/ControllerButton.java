package de.skyengine.core.input.controller;

public enum ControllerButton {
    CROSS_A, CIRCLE_B, SQUARE_X, TRIANGLE_Y,
    L1, R1, L2, R2, L3, R3,
    START, SELECT, SHARE, OPTIONS,
    UP, DOWN, LEFT, RIGHT,
    HOME
}
